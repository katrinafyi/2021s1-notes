# MATH2301 A2
### Kait Lam

## Question 1
> Let $U_1$ and $U_2$ be subspaces of a vector space $V$. Prove that $U_1 \cup U_2$ is a subspace of $V$ if and only if $U_1 \subseteq U_2$ or $U_2 \subseteq U_1$.

**Proof.** $(\impliedby)$ Clearly, if $U_1 \subseteq U_2$ or $U_2 \subseteq U_1$, then $U_1 \cup U_2 = U_1$ or $U_2$ and $U_1, U_2$ are both subspaces by assumption.

$(\implies)$ Assume $U_1 \cup U_2$ is a subspace of $V$. Consider $U_1 \setminus U_2$. If this set difference is empty, then $U_1 \subseteq U_2$ and the result holds. 

Assume $U_1 \setminus U_2$ is non-empty. Take $\mathbf u_1 \in U_1 \setminus U_2$ and any $\mathbf u_2 \in U_2$. We have $\mathbf u_1 + \mathbf u_2 \in U_1 \cup U_2$ because it is a subspace. Now considering if the sum is in $U_1$ or $U_2$,
$$
\begin{aligned}
\mathbf u_1 + \mathbf u_2 \in U_1 &\implies (-\mathbf u_1) + \mathbf u_1 + \mathbf u_2 = \mathbf u_2 \in U_1 \\ 
\mathbf u_1 + \mathbf u_2 \in U_2 &\implies \mathbf u_1 + \mathbf u_2 + (-\mathbf u_2)= \mathbf u_1 \in U_2
\end{aligned}
$$
Note the second case is impossible because we took $\mathbf u_1 \in U_1 \setminus U_2$ so $\mathbf u_1 \notin U_2$. Thus, we must have $\mathbf u_2 \in U_1$. This holds for all $\mathbf u_2 \in U_2$ , so  $U_2 \subseteq U_1$ and the result again holds.

## Question 2
> Give an example of $T \in \mathcal{L}\left(\mathbb{C}^{2}\right)$ such that $\operatorname{im}(T)=\operatorname{ker}(T)$.

Let $T : \mathbb C^2 \to \mathbb C^2$ be defined as 
$$
T(a+bi, c+di) = (0, a+bi).
$$
Clearly, 
$$
\operatorname*{im}T = \{(0, a+bi) \mid a, b \in \mathbb R\}.
$$
Moreover, $T(0, a+bi)=(0, 0)$ for all $a, b \in \mathbb R$ so
$$
\operatorname*{im} T = \operatorname*{ker} T
$$
as required.

> Show that there does not exist $T \in \mathcal{L}\left(\mathbb{R}^{5}\right)$ such that $\operatorname{im}(T)=\operatorname{ker}(T)$.

The rank-nullity theorem tells us that for any $T \in (\mathbb R^5)$, 
$$
\operatorname*{dim} \mathbb R^5 = 5= \operatorname*{null}T + \operatorname*{rank}T = \operatorname*{dim} \operatorname*{ker} T + \operatorname*{dim} \operatorname*{im} T.
$$
However, 5 is odd so this implies the nullity and rank (which must be nonnegative integers) are not equal.
$$
\operatorname*{dim} \operatorname*{ker} T \ne \operatorname*{dim} \operatorname*{im} T \implies \ker T \ne \operatorname*{im}T.
$$
This means there does not exist $T \in \mathcal{L}\left(\mathbb{R}^{5}\right)$ such that $\operatorname{im}(T)=\operatorname{ker}(T)$

## Question 3
> Let $T \in \mathcal{L}\left(P_{3}(\mathbb{C})\right)$ be defined by
> $$
> T(p(x))=p^{\prime \prime}(x)-p(0) .
> $$

>  Let $\gamma=\left\{x^{2}, x, x^{3}-1,1\right\}$ be an ordered basis and $\beta$ the standard ordered basis for $P_{3}(\mathbb{C})$. Determine the matrix representation $[T]_{\gamma}^{\beta}$.

The standard ordered basis is
$$
\beta = \{1, x, x^2, x^3\}.
$$

We will write the transformation of vectors in $\gamma$ in terms of vectors in $\beta$.
$$
\begin{aligned}
T(x^2) &= 2 - 0 \\ 
T(x) &= 0\\
T(x^3 - 1) &= 6x + 1 \\
T(1) &= -1
\end{aligned}
$$
Then, we can express this in a matrix,
$$
[T]_\gamma^\beta =\begin{bmatrix}
2 & 0 & 1 & -1 \\ 
0 & 0 & 6 & 0 \\ 
0 & 0 & 0 & 0 \\ 
0 & 0 & 0 & 0
\end{bmatrix}.
$$

> Determine a basis for $\operatorname{ker}(T)$.

Consider $T$ applied to a general polynomial 
$$
\begin{aligned}
p(x) &= a + bx + cx^2 + dx^3 \in P_3(\mathbb C), \\ 
\implies T(p(x)) &= -a + 2c + 6dx.
\end{aligned}
$$
We will solve for $T(p(x))=0$. 
$$
T(p(x)) = -a + 2c + 6dx = 0 \iff
\begin{cases}
0=d, \\
0=-a+2c,\\
b \in \mathbb C.
\end{cases}
$$
From this system of equations, we can see that it is solved by vectors of the form
$$
[p(x)]^\beta \in \{(2c, b, c, 0) \mid b, c, \in \mathbb C\}.
$$
Therefore, 
$$
\ker T = \operatorname*{span}\{2 + x^2, x\}
$$
and $\{2 + x^2, x\}$ forms a basis for $\operatorname*{ker}T$. Clearly, they are linearly independent.

## Question 4
> Let the linear transformation $T: M_{2 \times 2}(\mathbb{R}) \rightarrow P_{2}(\mathbb{R})$ be defined by
>$$
>T\left(\begin{array}{ll}
>a & b \\
>c & d
>\end{array}\right)=a+d+(b-c) x+(a-b+c+d) x^{2} .
>$$

> Find a basis for $\operatorname*{ker}T$.

First,
$$
\begin{aligned}
T
\begin{bmatrix}
a & b \\ c & d
\end{bmatrix} = 0 
&\iff 
\begin{cases}
0 = a+d \\
0 = b - c \\ 
0 = a - b + c + d
\end{cases}\\
&\iff
\begin{cases}
a=-d \\ 
b=c
\end{cases}
\end{aligned}
$$

From this, a basis for $\ker T$ is
$$
\left\{\begin{bmatrix}
1 & 0 \\ 0 & -1
\end{bmatrix}, \begin{bmatrix}
0 & 1 \\ 1 & 0
\end{bmatrix}  \right\}.
$$

> Find $A_1, \ldots, A_n \in M_{2 \times 2}(\mathbb R)$ such that $\{T(A_1), \ldots, T(A_n)\}$ is a basis for $\operatorname*{im} T$.

Using rank-nullity theorem,
$$
\operatorname*{dim}M_{2 \times 2}(\mathbb R)
= \operatorname*{null}T + \operatorname*{rank}T \implies \operatorname*{rank}T = 2
$$
so we should expect $n=2$ vectors in a basis for $\operatorname*{im}T$.

Let $\alpha = a + d$ and $\beta = b - c$ in
$$
\begin{aligned}
T \begin{bmatrix}
a & b \\ c & d
\end{bmatrix}&= (a+d) + (b-c)x + (a-b+c+d)x^2 \\ 
&= \alpha + \beta x + (\alpha-\beta)x^2 \\ 
&= \alpha(1+x) + \beta(x-x^2)
\end{aligned}
$$
Now, we can see that $\{1+x, x-x^2\}$ is a basis for $\operatorname*{im}T$ because any polynomial in the image can be written as a linear combination of the two vectors. It is easy to see these are linearly independent vectors.

## Question 5

>Let the distinct eigenvalues of a square matrix $A$ be denoted by $\lambda_{1}, \ldots, \lambda_{k}$. Suppose the corresponding algebraic multiplicities are $m_{1}, \ldots, m_{k}$ and that $A$ is similar to an upper-triangular matrix. Show that
>$$
\operatorname{tr}(A)=\sum_{i=1}^{k} m_{i} \lambda_{i} \quad \text { and } \quad \operatorname{det}(A)=\prod_{i=1}^{k}\left(\lambda_{i}\right)^{m_{i}}.
>$$

Let $A = Q^{-1}BQ$ where $B$ is an upper triangular matrix and $Q$ is an invertible matrix. Recall that similar matrices have the same characteristic polynomial and hence the same eigenvalues. 

For a triangular matrix, we can write the characteristic polynomial as
$$
f_B(t) = \operatorname*{det}(tI_n - B) = 
\left| \begin{matrix}
t-b_{11} & b_{12}& &\cdots&b_{1n}   \\ 
0 & t-b_{22} &b_{23}&&\vdots\\
 & 0&t-b_{33} &b_{34}\\
 \vdots&&\ddots&\ddots\\
 0&\cdots&&0&t-b_{nn}
\end{matrix}\right|.
$$
Then, we can use the recursive formula for determinant and choose to multiply $(t-b_{11})$ by the determinant of the $(n-1)\times(n-1)$ bottom-right submatrix. We evaluate the submatrix's determinant in the same way and we get
$$
f_B(t)=\operatorname*{det}(tI_n-B) = \prod_{i=1}^n(t-b_{ii}).
$$
An immediate consequence of this is the eigenvalues of a triangular matrix are exactly its diagonal entries.

Now, we will show that $\operatorname*{tr}AB = \operatorname*{tr}BA$ if $A$ and $B$ have the same shape.
$$
\operatorname*{tr}AB = \sum_{i=1}^n\sum_{j=1}^n a_{ij}b_{ji} = \sum_{j=1}^n \sum_{i=1}^n b_{ji}a_{ij} = \operatorname*{tr}BA.
$$
Here, note that $\sum_{j=1}^n a_{ij}b_{ji}$ is the $(i,i)$-th entry of $AB$, and $\sum_{i=1}^n b_{ji}a_{ij}$ is the $(j,j)$-th entry of $BA$, using ordinary matrix multiplication rules.

With this in mind,
$$
\operatorname*{tr}A = \operatorname*{tr}(Q^{-1}BQ) = \operatorname*{tr}(QQ^{-1}B) =\operatorname*{tr}B
= \sum_{i=1}^n b_{ii}= \sum_{i=1}^km_i\lambda_i
$$
because the sum of diagonals of a triangular matrix is the sum of its eigenvalues (possibly repeated by algebraic multiplicity), as shown earlier.

The determinant is similar. We know the determinant of a triangular matrix is the product of its diagonal entries (can be shown by setting $t=0$ in $f_B(t)$ above), so
$$
\operatorname*{det}B = \prod_{i=1}^n B_{ii} = \operatorname*{det}\prod_{i=1}^k \lambda_i^{m_i}.
$$
We have already shown by theorem 9.6 that similar matrices have the same determinant, so $\operatorname*{det}A = \det B$.

Therefore,
$$
\operatorname*{tr}A = \sum_{i=1}^km_i\lambda_i
\quad \text{and}\quad
\det A = \operatorname*{det}\prod_{i=1}^k \lambda_i^{m_i}
$$
as required.


## Question 6
>Let $A \in M_{n \times n}(\mathbb C)$ be diagonalisable with real eigenvalues only. Show that
>$$
\operatorname*{tr}A^2 = 0 \implies A = 0.
>$$

Let $A = Q^{-1}\Lambda Q$ where $\Lambda$ is a diagonal vector of $A$'s eigenvalues. Then, we have $A^2 = Q^{-1}\Lambda^2Q$. Furthermore, because similar matrices have the same eigenvalues and $A^2$ is similar to $\Lambda^2$, this means that the eigenvalues of $A^2$ are just the eigenvalues of $A$, squared.

Let $\lambda_1, \ldots, \lambda_n$ be the eigenvalues of $A$ (not necessarily distinct). Then, using the fact that the trace of similar matrices is the same (shown above),
$$
\operatorname*{tr}A^2 = 0 \implies \operatorname*{tr}\Lambda^2 = \sum_{i=1}^n \lambda_i^2=0.
$$
Because we have $\lambda_i \in \mathbb R$, we know $\lambda_i^2 \ge 0$. Therefore,
$$
\operatorname*{tr}\Lambda^2 = \sum_{i=1}^n \lambda_i^2=0 \iff \lambda_1=\cdots=\lambda_n=0 \iff \Lambda = \mathbf 0_{n \times n}.
$$
Hence,
$$
A = Q^{-1}\Lambda Q = Q^{-1}\mathbf 0 Q = \mathbf 0.
$$

## Question 7

>Let $U, V, W$ be finite-dimensional vector spaces over $\mathbf F$, and let $S \in \mathcal L(V, W)$ and $T \in \mathcal L(U,V)$. Show that
>$$
\operatorname*{rank}ST = \operatorname*{rank}T - \operatorname*{dim}(\operatorname*{im}T \cap \operatorname*{ker}S)
>$$

Define a new linear transformation, $S' \in \mathcal L(\operatorname*{im}T, \operatorname*{im}ST)$, which is the restriction of $S$ to $\operatorname*{im}T\subseteq$V. This is reasonable because the images are both vector spaces and furthermore, $\operatorname*{im}ST = S(\operatorname*{im}T)$.

Applying rank-nullity theorem,
$$
\operatorname*{dim}\operatorname*{im}T = \operatorname*{null}S' + \operatorname*{rank}S'.
$$
Clearly, for $\mathbf v \in \operatorname*{im}T$, we have $\mathbf v \in \operatorname*{ker}S' \iff \mathbf v \in \ker S$, so $\operatorname*{ker}S' =\operatorname*{ker}S \cap \operatorname*{im}T$. 

Now we want to show that $S'$ is surjective, that is $\operatorname*{im}ST = \operatorname*{im}S'$. Let $\mathbf w \in \operatorname*{im}ST$. Then, there exists $\mathbf u \in U$ such that $\mathbf w = S(T(\mathbf u))$. Then, $\mathbf v = T(\mathbf u) \in \operatorname*{im}T$ and $S'(\mathbf v)=\mathbf w$ so $S'$ is surjective. 

Putting this all together,
$$
\begin{aligned}
\operatorname*{dim} \operatorname*{im}T &=  \operatorname*{null}S' + \operatorname*{rank}S' \\ 
\implies\operatorname*{rank}T &=\operatorname*{dim} \operatorname*{ker}S' + \operatorname*{dim} \operatorname*{im}S'\\
&=\operatorname*{dim} (\operatorname*{ker}S \cap \operatorname*{im}T) + \operatorname*{dim} \operatorname*{im}ST\\
&=\operatorname*{dim} (\operatorname*{ker}S \cap \operatorname*{im}T) + \operatorname*{rank}ST\\ 
\implies \operatorname*{rank}ST &= \operatorname*{rank}T - \operatorname*{dim}(\operatorname*{ker}S \cap \operatorname*{im}T)
\end{aligned}
$$
as required.

## Question 8
> Let $V$ be a finite-dimensional complex vector space. For each subspace $U \subseteq V$, define $U^{\prime}=\{f \in \mathcal{L}(V, \mathbb{C}) \mid f(U)=\{0\}\}$. For subspaces $U_{1}, U_{2} \subseteq V$, show that
>$$
U_{1}^{\prime} \cap U_{2}^{\prime}=\left(U_{1}+U_{2}\right)^{\prime}.
>$$

First, we will show $U_1'\cap U_2' \subseteq (U_1+U_2)'$. Let $f \in U_1' \cap U_2'$ so $f(U_1)=f(U_2)=\{0\}$. Let $\mathbf u_1 \in U_1$, $\mathbf u_2 \in U_2$, then
$$
f(\mathbf u_1 + \mathbf u_2) = f(\mathbf u_1) + f(\mathbf u_2) = 0 + 0 = 0
$$
because $f$ is a linear transformation. Thus,
$$
f(U_1 + U_2) = \{0\} \implies f \in (U_1 + U_2)'.
$$

Now to show $U_1'\cap U_2' \supseteq (U_1+U_2)'$. Take $f \in (U_1+U_2)'$ so $f(\mathbf u_1 + \mathbf u_2)=0$ for all $\mathbf u_1 \in U_1, \mathbf u_2 \in U_2$. In particular, we can set $\mathbf u_1=0$ then $\mathbf u_2=0$ to get
$$
\begin{aligned}
0 &= f(\mathbf u_1 + 0)=f(\mathbf u_1) + f(0)=f(\mathbf u_1), \\ 
0 &= f(0 + \mathbf u_2)=f(0) + f(\mathbf u_2)=f(\mathbf u_2).
\end{aligned}
$$
This implies $f \in U_1'$ and $f \in U_2'$.

We conclude $U_1' \cap U_2' = (U_1+U_2)'$.
