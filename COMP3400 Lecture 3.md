# Strings
This declares x to be a single character and xs to be a list of character.
```
x = 'G'
xs = "Grogu"
```

## Do

The do syntax allows us to sequence functions like to sequence of function calls which comprise the main program. This is needed because Haskell modules are unordered.

